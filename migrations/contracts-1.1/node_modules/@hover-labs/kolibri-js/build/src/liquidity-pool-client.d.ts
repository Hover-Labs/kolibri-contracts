import Address from './types/address';
import { TransactionWalletOperation } from '@taquito/taquito';
import { ThanosWallet } from '@thanos-wallet/dapp';
import { InMemorySigner } from '@taquito/signer';
import { TransactionOperation } from '@taquito/taquito/dist/types/operations/transaction-operation';
export default class LiquidityPoolClient {
    readonly liquidityPoolAddress: Address;
    private readonly tezos;
    constructor(nodeUrl: string, wallet: InMemorySigner | ThanosWallet, liquidityPoolAddress: Address);
    liquidate(targetOvenAddress: Address): Promise<TransactionOperation | TransactionWalletOperation>;
}
