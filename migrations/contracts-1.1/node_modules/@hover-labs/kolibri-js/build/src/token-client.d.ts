import Shard from './types/shard';
import Address from './types/address';
export default class TokenClient {
    private readonly tokenAddress;
    private readonly tezos;
    constructor(nodeUrl: string, tokenAddress: Address);
    getBalance(address: Address): Promise<Shard>;
}
