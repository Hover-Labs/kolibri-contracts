import Address from './types/address';
import Mutez from './types/mutez';
export declare type HarbingerPriceFeedData = {
    time: Date;
    price: Mutez;
};
export default class HarbingerClient {
    readonly normalizerAddress: Address;
    private readonly tezos;
    constructor(nodeUrl: string, normalizerAddress: Address);
    getPriceData(): Promise<HarbingerPriceFeedData>;
}
