import { LogLevel } from './common';
export default function revokeOracle(logLevel: LogLevel, signedRevokeCommand: string, oracleContractAddress: string, revokerPrivateKey: string, tezosNodeURL: string): Promise<void>;
